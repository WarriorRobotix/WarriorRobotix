<h1 class="flow-text">Listing Competitions</h1>
<% if member_is_admin? %>
<%= link_to 'New Competition', new_competition_path, class: "btn-bordered" %>
<% end %>

<div class="row">
  <% @competitions.each do |competition| %>
  <%= render competition %>
  <% end %>
</div>
